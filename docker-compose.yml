version: '3'

services:

  gat-image-server:
    container_name: gat-image-server
    image: gat-image-server:latest
    ports:
      - "9090:9090"
    volumes:
      - /var/www/tiling-images/trees:/var/www/images
    restart: unless-stopped